# API Configuration
REACT_APP_API_URL=http://localhost:5000/api

# Note: All environment variables starting with REACT_APP_ are embedded into the build.
# To use these in production, you must set them during the build process.
